<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/loginButton"
        android:title="Login"
        android:onClick="open_login_activity"
        android:icon="@drawable/ic_face"
        />
    <item
        android:id="@+id/registerButton"
        android:title="Register"
        android:visible="true"
        android:onClick="open_register_activity"
        android:icon="@drawable/ic_person_add"
        />
    <item
        android:id="@+id/history"
        android:title="History"
        android:icon="@drawable/ic_history"

        android:onClick="local_history"
        />
    <item
        android:id="@+id/local_history"
        android:title="Local History"
        android:onClick="local_history_activity"
        android:icon="@drawable/ic_history"
        />
    <item
        android:id="@+id/logout_item"
        android:title="Logout"
        android:icon="@drawable/ic_history"
        android:onClick="logoutClicked"
        android:visible="false"
        />
</menu>